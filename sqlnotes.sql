;A. ALTER TABLE lets you add column to a table in db
;-----------------------------------------------------------------------
ALTER TABLE test
ADD r6 TEXT;
;-----------------------------------------------------------------------
; B. AND is an operator that combines two conditions. Both conditions must
;    be true for the row to be included in the result set.

;-----------------------------------------------------------------------
